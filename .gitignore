# Confidential — never push to GitHub (Google Drive + LMS only)
assessment/

# Large source references (copyrighted study guides + 200MB master deck) — local only
reference/

# OS / tooling junk
.DS_Store
**/.DS_Store
courseare/

# Distribution archives (rebuildable — never pushed to GitHub)
*.zip
**/reference/

# Build toolchain (skills, hooks, push scripts) — local only, not part of the published course
.claude/

# Lab asset generator (build tooling — local only)
labs/__pycache__/
labs/build_lab_assets.py
labs/lab_assets_data.py

# Superseded versions kept locally only — never pushed
archive/
